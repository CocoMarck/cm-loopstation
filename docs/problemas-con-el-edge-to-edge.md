# Problemas con el Edge-to-Edge
Los celus android 15-16, forzan esto. El problema que generan, es que el programa se dibuja debajo del especio reservado para los botones de control, `triangulo, circulo, cuadrado`, y `hora tiempo`.

Si se detecta un celu que jala con esto, se tiene que hacer un padding.
